close all;

% data = load('mnist_train.csv');
% 
% labels = data(:,1);
% 
% images = data(:,2:785)

tic;% Display elasped time in seconds 
namelist = strings;
index=1;
for i = 1:500
    if labels(i,1)==5
        str = ['five_',num2str(i),'.png'];
        imwrite(reshape(images(i,:),28,28)',str)
    end
end
toc;